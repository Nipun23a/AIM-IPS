# Trained Models

Stores serialized trained models (e.g., `.pkl`, `.pt`, `.h5`) for anomaly detection and classification.

## Structure

- `anomaly_model.pkl`
- `classifier_model.pt`
- `shap_explainer.pkl`

## Notes

Ensure large files are tracked with Git LFS or excluded via `.gitignore`.
