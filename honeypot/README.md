# Honeypot Module

This directory includes fake services or endpoints (e.g. SSH, HTTP) to attract and capture potentially malicious behavior for training and evaluation.

## Components

- Cowrie or Dionaea integration
- Optional: Custom HTTP endpoints (Flask)

## Output

- Logs of connection attempts
- Payloads or requests saved for feature extraction
