# Preprocessing & Feature Engineering

Processes raw logs into structured features usable by ML models.

## Tasks

- Cleaning & normalization
- Feature extraction (e.g., request size, frequency, user-agent entropy)
- Categorical encoding & scaling

## Output

- Feature vectors saved as `.csv` or `.npy`
